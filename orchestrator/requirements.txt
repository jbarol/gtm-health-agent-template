anthropic>=0.92.0
slack-bolt>=1.21.0
slack-sdk>=3.33.0
httpx>=0.27.0
apscheduler>=3.10.0
psycopg2-binary>=2.9.9
quickchart-io>=2.0.0
simple-salesforce>=1.12.0
pydantic>=2.0,<3
pyyaml>=6.0
openpyxl>=3.1
duckdb>=0.10.0
pyarrow>=14.0.0
# numpy + pandas required for DuckDB's ``conn.execute(sql).df()`` path used
# by orchestrator/artifact_query_tool.py to convert query results to a
# DataFrame for inline-vs-virtualize routing. Without these, every
# query_artifact call falls into the error branch with "No module named
# 'numpy'" and the Coordinator retries until the session blows past the
# 1M-token cap — the exact failure seen 2026-05-13 ~08:54 PT.
numpy>=1.26.0
pandas>=2.0.0
compresr>=2.5.0
python-docx>=1.1.0
# boto3: S3-compatible upload for the snapshot Parquet cold archive
# (orchestrator/snapshot_archive.py, incident 2026-06-16). Imported lazily,
# so disabled-mode (ARCHIVE_BUCKET_ENABLED unset) never touches it.
boto3>=1.34.0
